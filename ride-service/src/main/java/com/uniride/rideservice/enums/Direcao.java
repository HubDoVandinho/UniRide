package com.uniride.rideservice.enums;

/**
 * Direção da rotina/carona em relação à instituição.
 * IDA   = casa → instituição (origem = residência do motorista)
 * VOLTA = instituição → casa (destino = residência do motorista)
 */
public enum Direcao {
    IDA,
    VOLTA
}
